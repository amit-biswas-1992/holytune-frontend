import React from 'react';
import { useRouter } from 'next/router';
const Testid = () => {
    // const router = useRouter();
    // console.log(router);

    return (
        <div>

        </div>
    );
};

export default Testid;